{
  "name": "scenario-09-hardened-container",
  "version": "0.1.0",
  "private": true,
  "description": "Verify Docker container hardening: dropped capabilities, read-only filesystem, resource limits, non-root user.",
  "scripts": {
    "test": "node verify.js"
  }
}
