{
  "name": "Claude Code Sandbox - Devcontainer",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "features": {},
  "postCreateCommand": "npm install",
  "initializeCommand": "",
  "remoteUser": "node",
  "runArgs": [
    "--cap-drop=ALL",
    "--security-opt=no-new-privileges",
    "--read-only",
    "--tmpfs=/tmp:rw,noexec,nosuid",
    "--tmpfs=/home/node:rw,nosuid"
  ]
}
